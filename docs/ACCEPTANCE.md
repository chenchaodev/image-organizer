# 迭代 1「M0 骨架」 验收记录

> 验收清单与实测记录。迭代规划时写待实测清单初稿;交付后交用户实测,反馈通过 → 记录通过;反馈 bug → 独立修复迭代,修复后标「待复测」。条目按迭代追加:已勾选历史明细长期保留(作回归测试用例来源),不重复列入新规划;单次规划新增 ≤5 项(小型豁免迭代可不建清单,见全局配置目录 AGENTS.md)。可自动断言的功能项不写本清单,只写「自动断言见 test/segments/X.test.js」指针,本清单只列人工 GUI 实测项。

## 待实测清单(按迭代追加;历史勾选保留作回归用例,新增 ≤5 项)
- [ ] 启动:运行 `npm run tauri dev`,应用窗口出现且标题为 Image Organizer
- [ ] IPC:界面上显示 Rust 后端返回的版本信息(如 app version),证明前端↔后端链路通
- [ ] 图库目录:点击「选择图库目录」能选定一个文件夹,界面显示已选中路径
- [ ] SQLite:选定目录后创建/连接库文件成功,重启应用后仍能识别该图库(不报错)
- 自动断言见 test/segments/(后续迭代建立测试段)

## 实测结果记录
- (迭代完成交付实测后填写)
